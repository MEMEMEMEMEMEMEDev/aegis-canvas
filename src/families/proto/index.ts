// =============================================================================
// Familia PROTO — incubadora VACÍA, lista para gestar la próxima familia.
// Flujo probado (así nació TEBEO):
//   1. Referencias visuales en refs/ (raíz del repo) → se analizan en sesión.
//   2. La familia emite su voz en tebeo.scss-style: fuentes self-hosted
//      (src/fonts/README.md) + mini-contrato scoped (.proto-scope) si es isla.
//   3. Componentes = piel sobre los núcleos headless (primitives/ +
//      behaviors/), un set pequeño primero, luego la vista completa en demo/.
//   4. Cuando la identidad cuaja: se bautiza y se renombra todo (PROTO es
//      siempre provisional).
// =============================================================================
export {};
